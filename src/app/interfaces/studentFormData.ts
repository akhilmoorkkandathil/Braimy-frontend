export interface StudentData {
    username: string;
    phone: string;
    class: string;
    email: string;
    tutor: string;
    coordinator: string;
    course: string;
    photoUrl?: string; // Added this based on your previous code
  }
  